const path = require('path');

module.exports = {
  "config": path.resolve(__dirname, "src/config/database.config.ts"),  // Sequelize config file
  "models-path": path.resolve(__dirname, "src/models"),      // Models location
  "seeders-path": path.resolve(__dirname, "src/seeders"),    // Seeders location
  "migrations-path": path.resolve(__dirname, "src/migrations") // Migrations location
};
